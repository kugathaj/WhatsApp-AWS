import { Text, View } from '../../components/Themed';

export default function CallsScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center', }}>
      <Text style={{ fontSize: 20, fontWeight: 'bold' }}>CALLS SCREEN</Text>
    </View>
  );
}